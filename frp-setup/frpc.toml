# frpc.toml — Shreerang Trendz FRP Client Config
# Save to: C:\frp_0.58.1_windows_amd64\frpc.toml
# Run: frpc.exe -c frpc.toml  (keep window open!)

serverAddr = "72.61.249.86"
serverPort = 7000

[auth]
method = "token"
token = "ShreerangFRP2026"

[[proxies]]
name = "tally"
type = "http"
localIP = "127.0.0.1"
localPort = 9000
customDomains = ["tally.shreerangtrendz.com"]
