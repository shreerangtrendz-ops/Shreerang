# frpc.toml — Shreerang Trendz FRP Client Config
# Copy this file to: C:\frp_0.58.1_windows_amd64\frpc.toml
# Then run: frpc.exe -c frpc.toml

serverAddr = "72.61.249.86"
serverPort = 7000

# Optional: auth token (set this if frps has auth enabled)
# auth.token = "your_token_here"

[[proxies]]
name = "tally"
type = "http"
localIP = "127.0.0.1"
localPort = 9000
customDomains = ["tally.shreerangtrendz.com"]
